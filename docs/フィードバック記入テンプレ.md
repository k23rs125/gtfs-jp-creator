# 試用フィードバック（記入テンプレ）

このファイルをコピーして `<…>` を埋め、GitHub ハブ経由で返してください（Issue 起票 or handoff md）。
書ける所だけでOK。所要 5〜10分。

---

- 記入者: `<名前>`  / 試用日: `<YYYY-MM-DD>` / 所要: `<分>`
- 環境: OS `<Windows / Mac / Linux>` / Python `<3.x>` / Java `<有 / 無>`

## 1. 試した時刻表
- 自治体・路線: `<例: ○○市 ○○線 / 同梱サンプル>`
- 形式: `<PDF / Excel / 画像 / .pub>`
- 規模: 停留所 `<N>` ／ 便 `<N>` ／ 往復・循環 `<往復 / 循環 / 片道>`

## 2. セットアップ（つまずき）
- `pip install`: `<OK / 詰まった点>`
- Java ＋ Validator jar: `<OK / 詰まった点 / 未実施>`
- その他（手順の不足・分かりにくさ）: `<>`

## 3. 抽出の正確さ
- 停留所・便・時刻の取りこぼし／取り違え: `<無 / 有: 具体的に>`

## 4. 条件確認（ask-first）
- 質問は分かりやすかったか: `<はい / いいえ + 理由>`
- 過不足（聞かれすぎ・足りない）: `<>`

## 5. 生成・検証
- 標準 Validator ERROR: `<N>` ／ JP拡張 ERROR: `<N>` ／ 内部整合: `<N / N>`
- 完走したか: `<完走 / 途中で停止: どのStepで何が>`

## 6. 出力の妥当性
- 地図（map_view）で停留所・経路: `<妥当 / 飛びあり: どの停留所>`
- 座標の未補完・要確認: `<件数・内容>`

## 7. 所感
- 一番良かった点: `<>`
- 一番の改善要望: `<>`
- 使いやすさ（5=とても良い 〜 1=悪い）: `<1〜5>`
- 自由記述: `<>`
