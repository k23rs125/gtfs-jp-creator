# Docker イメージに入れないもの（サイズ削減・不要物除外）
.git
.github
**/__pycache__
**/*.pyc
.venv
venv
node_modules
# 研究物・大きい成果物（アプリ実行には不要）
*.pptx
*.mp4
*.pdf
*.zip
baseline_output
enrich_results
eval_report*.md
test_demo
p11_data          # P11キャッシュは実行時に取得（イメージに焼かない）
# 注: 検証用 jar (skills/gtfs-jp-creator/tools/gtfs-validator-cli.jar) は
#     .gitignore 対象だが Docker には含める必要があるため、ここでは除外しない。
