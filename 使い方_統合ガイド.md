# GTFS-JP Creator 使い方・統合ガイド
### 〜 人間向けの導線 ＋ ツール付きLLMの司令書 を1枚に 〜

このガイドは **バス時刻表（PDF / Excel）から GTFS-JP データを半自動生成する** ための単一の手引きである。
2通りの読み方ができる。

- **(A) 人間として手順を追う** … 第1部を読む。何ができ、何を準備し、どう実行するか。
- **(B) ツール付きLLMに実行させる** … 本ガイドごと Claude Code 等（スクリプトを実行できるLLM）に渡す。LLMは第2部の司令書に従う。

---

## 設計の核心（最初に必ず読む）

**LLMは「構造化（Step2）」と「司令役」だけを担う。抽出・各ファイル生成・座標補完・走行経路・検証は、すべて決定的なスクリプトが行う。**

> ⚠️ **「PDFをLLMに貼るだけで全部作る」方式は採用しない。**
> LLM単体に丸投げすると、長い表の末尾省略・時刻の取り違え・**座標の捏造**・ファイル間の相互参照崩れが起き、信頼できる GTFS にならない。
> 量が多く正確さが要る部分は決定的処理に任せ、LLMは「紙面の解釈（路線・方向・循環の判断）」と「手順の進行・利用者への質問」に限定する。これが本ツールの再現性・誤差ゼロの源である。

決まらない値は **推測で埋めず**、利用者に質問する（「正しく失敗」）。

---

# 第1部：人間向け — 何ができて、何が要るか

## 1.1 入力と出力
- **入力**：バス時刻表（テキストPDF / Excel(.xlsx) / 装飾PDF / 画像PDF）
- **出力**：GTFS-JP 一式（zip。JP拡張 agency_jp / routes_jp 等を含む）＋ 検証レポート ＋ 地図ビュー(HTML)

## 1.2 必要なもの（環境）
| 用途 | 必要物 | 備考 |
|---|---|---|
| 抽出・生成・座標補完（主経路） | Python ＋ `pip install -r requirements.txt` | pdfplumber / pyshp / requests / pykakasi |
| 座標補完 P11 | 国土数値情報 P11（**対象都道府県**の SHP） | `download_p11.py <都道府県>` で取得 |
| 走行経路 shapes | OSRM エンドポイント（ネット） | `/route` を利用 |
| 検証 | Java 17 以上 ＋ gtfs-validator-cli.jar | `tools/` に配置 |
| 装飾PDFの抽出（任意） | MinerU（GPU推奨） | CPUは30〜60分。テキストPDFなら不要 |

## 1.3 全体の流れ（LLMを使うのは Step2 だけ）
```
[Step1] 時刻表(PDF/Excel)  → 抽出（座標方式 / Excel直接 / OCR）         ※決定的
[Step2] 抽出結果           → 構造化中間表現 JSON                        ※LLM（路線・方向・循環の判断）
[条件確認] JSON            → 要入力サマリを提示し、利用者が不足項目を補完  ※質問は一度に集約
[Step3]  JSON             → GTFS-JP 各ファイル（JP拡張含む）             ※決定的
[Step3.5] stops           → 緯度経度補完（旧feed→P11→Nominatim→手動→経路内挿※推定） ※決定的・手動最優先
[Step4]  stop_times       → shapes.txt（OSRM routing）                  ※決定的
[Step6]  stops/routes     → translations.txt（ふりがな pykakasi / 英訳） ※決定的（一部LLM）
[Step5]  全ファイル        → zip パッケージング                          ※決定的
[Step7]  zip              → 検証（MobilityData ＋ JP拡張）＋内部整合検証   ※決定的
```

## 1.4 利用者が決めること（PDFに無い項目は質問される）
時刻表に載っていない項目は推測されず、**条件確認サマリ**でまとめて尋ねられる。
- 事業者情報（正式名称・所在地・電話・URL・連絡先メール）
- 運行する曜日 ／ フィードの有効期間 ／ **運休日（祝日・年末年始・お盆）**
- **運賃**（一律、**大人/小児/障がい者の区分別**、または **区間制(zone)**）
- 自動補完できない座標（同名あいまい・接尾辞付き名称など）
- 路線名・方向・循環の割り当て（紙面の解釈が要る点。多路線＝往復が同一停留所集合なら自動でまとめる）
- **複数ダイヤ**（平日/土日で時刻が違う場合は運行日ごとに別カレンダー）
- 要予約・特殊便の扱い

**入力が `.pub`（Microsoft Publisher）のとき**：直接読めない（OLE2複合）。Publisher本体があれば COM で PDF化してから座標方式にかける（`$pub=New-Object -ComObject Publisher.Application; $doc=$pub.Open($src); $doc.ExportAsFixedFormat(2,$out,1)`）。テキスト保持PDFなら誤差ゼロ抽出可。詳細は SKILL.md Step1。

## 1.5 最短手順（テキストPDFの例・4コマンド）
```powershell
# 1. 抽出（座標方式・該当ページ指定。2方面は見出し行で自動縦分割）
python skills\gtfs-jp-creator\scripts\extract_timetable_coords.py "時刻表.pdf" -o test_demo\extract.json --page 2

# 2. 構造化（LLMが extract.json → claude.json を作成。条件確認で不足項目を補完）

# 3. 生成〜検証を一括（config を指定）
python skills\gtfs-jp-creator\scripts\run_pipeline.py --config test_demo\pipeline_config.json

# 4. 内部整合検証（抽出した時刻が stop_times まで保たれているか）
python skills\gtfs-jp-creator\scripts\verify_stop_times_vs_extract.py test_demo\extract.json --gtfs test_demo\out\gtfs
```

**画像化PDF（スキャン）のとき**：座標方式は使えないので OCR 経路へ。
`pdf_to_markdown.py --engine mineru`（既定 pipeline で数字に正確・数分）→ `extract_timetable_markdown.py` で
extract.json 化 → 以降は同じ。生成後は `時刻アノマリ.json` でOCR誤読の疑いを確認する。

**画面で操作したい場合**：半自動アプリ（`streamlit run app/app.py`）。抽出→路線割り当て→全時刻の目視確認→
条件入力→生成→**地図でピンをドラッグして座標確定**→7タブビューア。詳細は [app/README](app/README.md)。

---

# 第2部：LLMオーケストレーション司令書

> **あなた（LLM）への指示**
> あなたの役割は **(1) 構造化（Step2）** と **(2) 手順の進行・利用者への質問** である。
> 抽出・生成・座標補完・shapes・検証は **下記スクリプトを実行**して行い、結果を解釈する。
> 決まらない値は推測せず、条件確認サマリで利用者にまとめて質問する。各段で品質ゲートを必ず確認する。
> Windows では cp932 クラッシュ回避のため `PYTHONIOENCODING=utf-8` を付けて実行する。

## 起動手順（このリポジトリURLとPDFを渡されたAI向け）
**前提：あなたはスクリプトを実行できる（shell＋ネットがある）こと。** そうでなければ正確に作れないので、利用者に「コードを実行できる環境（Claude Code 等）が必要」と伝えて止まる。

> ⚠️ **スクリプトを必ず実行する。読んで要約したり、出力を“それらしく”捏造（模倣）したりしない。** これは決定的処理に任せることが正確さの条件だからである。

URLとPDFを渡されたら、まず環境を自走で整える：
```bash
# 1. 取得と依存インストール
git clone https://github.com/k23rs125/gtfs-jp-creator
cd gtfs-jp-creator
pip install -r skills/gtfs-jp-creator/requirements.txt

# 2. 座標補完用 P11 を「対象地域の都道府県」で取得（例：福岡県）
python skills/gtfs-jp-creator/scripts/download_p11.py 福岡県 --year 22

# 3. （検証する場合）gtfs-validator-cli.jar を tools/ に配置（Java 17 以上が必要）
```
以降は本ガイドの **S1 → S2（条件確認で利用者に質問）→ S3 → S4（品質ゲート）→ S5** を順に実行する。

## S0. 前提確認
- リポジトリ位置・Python環境・**対象地域の都道府県**（P11取得に必須）を確認する。

## S1. 抽出（決定的・スクリプト実行）
入力形式で経路を選ぶ。
- **テキストPDF**：`extract_timetable_coords.py <pdf> -o extract.json [--page N]`
  - ページ未指定なら時刻トークン最多のページを自動選択。`--page` で明示すると確実。
  - 弓削線のような **同一時刻列の2方面** は、括弧付き方面見出し行で自動的に縦分割される。
  - 画像化PDF（文字なし）は警告し OCR 経路へ誘導される。
- **Excel（停留所が行・便が列）**：`extract_timetable_excel.py <xlsx> -o extract.json [--sheet 名 --name-col A]`
  - 表の途中に**方向見出し（「○○行き」）や便ヘッダ（「N便」/便番号/上り下り）**がある往復・上下
    レイアウトは、その行で**縦分割して方向別ブロック**にする（同一時刻列に縦積みの別方向便の連結を防ぐ）。
    方向見出しは `block.direction_hint` に入る。実証: 太宰府まほろば号（市役所行き4便＋湯の谷2行き6便）。
- **Excel（停留所が列・便が行＝転置）**：`extract_excel_transposed.py <xlsx> -o extract.json [--header-row 2]`
  - 各シート＝1方向。JR接続時刻・所要時間など停留所でない列は見出しパターンで自動除外。
- **装飾PDF**：MinerU → Markdown → あなたが構造化。
- 出力 `extract.json` の **`needs_confirmation` を必ず読む**（時刻逆行＝要予約寄り道／番号なし候補＝案内文混入 など）。

抽出JSON（座標方式）の便は `cells:[{seq,num,name,time,reserve}, ...]` を持つ。**num＝停留所番号、reserve＝要予約フラグ** が使える。

## S2. 構造化（あなたの仕事）＋ 条件確認
`extract.json` ＋ 利用者回答 → **構造化中間 JSON** を作る。スキーマ：
```
agency, agency_jp, office_jp, routes[], stops[], trips[], stop_times[],
calendar[], calendar_dates[], fare_attributes[], fare_rules[], _meta
```
構造化の鉄則：
1. **停留所は「名称」で区別する。** 同じ番号でも名称が違えば別の物理停留所（例「○○前（支所側）」と「（銀行側）」＝道路の反対側）。番号だけで統合しない。
2. **要予約バス停（reserve=True）は既定で除外**（利用者が残すと言えば別）。
3. **案内文の混入**（「お問い合わせ」欄が名前列に入る等。`needs_confirmation` に出る）は停留所にしない。
4. **便名・方向(direction_id)・循環** は座標から決まらない。原典と照合し、利用者承認のうえ割り当てる。
5. 時刻は `HH:MM:SS`、`arrival=departure`。stop_sequence は1始まり。
6. **循環路線の `trip_headsign` は方向名**（例「左回り／右回り」）にする。最終停留所名にしない
   （循環は始終点が同じため、最終停留所では行先が分からない）。Excelのシート名が方向名なら流用する。
7. **agency_id を全テーブルで揃える。** `agency` / `agency_jp` / `fare_attributes` の `agency_id` は
   同一にする（`AGENCY_TBD` 固定にしない）。Step3 が `fare`・`agency_jp` の `agency_id` を
   `agency.txt` のものへ自動統一するが、構造化側でも揃えておく（desync・foreign_key違反の防止）。

**条件確認サマリ**を提示し、🟦自動検出 / 🟨自動補完 / 🟧要入力 に分類。

**順序の原則**：PDF/Excelに無い情報は、**Web検索や仮値の前に、まず利用者に一括で質問する**。
逐次に小出しで聞かず、1枚のサマリで以下の **8項目を一括質問**する（推測で勝手に埋めない）。
1. 事業者（自治体/運行事業者・名称・所在地・電話・URL・連絡先メール）
2. 運行する曜日／運休日（祝日・お盆・年末年始）
3. 有効期間（開始・終了）
4. 路線名・方向・循環の割り当て
5. 運賃（一律/区間/無料）
6. 旧GTFS-JPフィードの有無（座標再利用）。**利用者回答だけに頼らず公式GTFSを能動確認**（下記）
7. 要予約・特殊便の扱い
8. 対象自治体の確認（P11・自治体制約用）

**公式GTFSの能動確認**：⑥は「なし」回答でも実在することがある（柳川市の例）。**BODIK(`data.bodik.jp` を自治体名/コードで検索)・自治体オープンデータ**を必ず確認し、見つかれば座標再利用・agency確定・精度比較に使える。発見は利用者に報告し使うか確認する（勝手に上書きしない）。公式が手元PDFより古い版なら座標は再利用・ダイヤは新しい手元を優先。

**「不明」と答えられた項目**：その項目を**「不明」と明記**し、当方がWebで調べて暫定値を埋める。
ただし埋めた値には必ず **「不明と回答されたため当方で調査した暫定値・正確とは保証できない（要確認）」**
の一文を添える。最後に要確認項目をまとめて提示する。

## S3. 生成〜検証（決定的・run_pipeline.py）
config JSON を作る：
```json
{
  "feed_name": "...", "input_json": "test_demo/claude.json",
  "output_dir": "test_demo/out", "context": "○○県○○市",
  "p11_shapefile": "p11_data/P11-22_40_SHP/P11-22_40_SHP/P11-22_40.shp",
  "use_nominatim": true,
  "manual_coords": "test_demo/manual_coords.json",
  "interpolate_coords": false,
  "validate": true
}
```
- ⚠️ **P11 は対象地域の都道府県 SHP を使う。** 無ければ `download_p11.py <都道府県> --year 22` で取得。
  **県を取り違えると、全国に同名のあるバス停が遠方に誤マッチする**（例：福岡=40 を 沖縄=47 と取り違え → 「高良」が沖縄にマッチ）。
- P11 照合は config の `context`（例: 福岡県久留米市）から **自治体範囲に自動で絞り込む**（`--municipality`）。
  これで**県内の同名別自治体への誤マッチ**（長福寺が筑前町に等）を手動なしで防ぐ。
- `--dry-run` で計画確認 → 本実行。座標補完は 旧feed→P11→Nominatim→**手動(最優先)** の順。
- `"interpolate_coords": true`（**既定OFF**）にすると **Step 3.5e** で、埋まらない停留所を便の停留所順で
  前後の既知座標から内挿する。**内挿値は「推定（要確認）」**（誤差中央値約146m・9割が500m以内）で、
  `work/interpolate_report.json` に一覧化される。市域外に出た内挿は外れ値として採用しない。
  「正しく失敗」を崩さないため、推定を許容できる場合のみ有効化する。

## S4. 品質ゲート（必須・省略不可）
1. **内部整合検証**：`verify_stop_times_vs_extract.py extract.json --gtfs out/gtfs`
   → `[PASS] N/N=100%` を確認（抽出時刻が stop_times まで保たれている証拠）。
   config に `extract_json`（blocks/cells形式の抽出JSON）を指定すると **run_pipeline の Step 7c**
   として自動実行され（`--strict`で不一致はFAIL）、`output_dir/stoptimes_verify.{md,json}` に出力。
2. **座標の外れ値チェック**：全停留所座標の中心から大きく外れる点を探す。
   医院・寺・店名など **全国に同名がある停留所** は誤マッチしやすい（例：長福寺が2自治体に存在）。
   接尾辞付き名称（「○○前（支所側）」）は P11 照合を外すので **手動座標(by_stop_name)** で補う。
3. **検証 ERROR 0**：MobilityData と JP拡張がともに ERROR 0 か。残る WARNING の意味を利用者に説明する
   （例：`missing_feed_contact_email_and_url` は事業者未定のため／`stop_too_far_from_shape` はコミュニティバスで発生しうる軟警告）。
4. **推定座標の要確認**：`interpolate_coords` を有効にした場合、`work/interpolate_report.json` の
   `interpolated_estimated`（推定座標）を利用者に提示し、原典・地図での確認を促す。
   推定は正確な座標ではない（誤差中央値約146m）ため、確定値として扱わない。
5. **同名複数候補の要確認**：`output_dir/座標_要確認.csv` が生成されていたら、
   そこに載った停留所は **P11に同名候補が市域内で複数あり、別地点の疑い**がある（先頭を仮採用済み）。
   **黙って確定せず、利用者に「どちらの○○か」を質問**して位置を確定する（無ければこのCSVは生成されない）。

## S5. 成果物の集約
`output_dir`（feedフォルダ）に **zip・gtfs/・validation/・stoptimes_verify.*・map_view.html**（＋同名複数候補があれば **座標_要確認.csv**）を揃える。
run_pipeline では **map_view.html は Step 4b で常時自動生成**、**stoptimes_verify.\* は Step 7c（`extract_json` 指定時）で自動生成**される（座標_要確認.csv は P11 で同名複数候補が出たときのみ）。

---

# 付録A：条件確認で必ず聞くこと（チェックリスト）
- [ ] agency：事業者名／GTFSのagencyに自治体・運行事業者どちらを使うか
- [ ] agency_jp：正式名称・郵便番号・所在地・URL・連絡先メール
- [ ] 運行する曜日（calendar）／運休日（お盆・年末年始等は calendar_dates）
- [ ] 有効期間（feed_start / feed_end）
- [ ] 路線名・方向・循環の割り当て
- [ ] 要予約・特殊便の扱い
- [ ] 自動補完できなかった座標、同名あいまいの選択

# 付録B：よくある落とし穴（実例）
| 症状 | 原因 | 対処 |
|---|---|---|
| 一部の座標が遠方（県外）に付く | P11の**県取り違え** | 対象県のP11を `download_p11.py` で取得し直す |
| 同名停留所が誤った場所に付く（県内別自治体） | P11に自治体制約が無い | `context` から自動で `--municipality` 制約（手動不要）。なお外れ値チェックも実施 |
| 接尾辞付き停留所が未補完 | 「（支所側）」等でP11照合を外す | 手動座標(by_stop_name)で補う／`interpolate_coords`で推定補完 |
| POI(店舗等)が補完できずERROR | OSM/P11に未収録（データ可用性の限界） | `interpolate_coords: true` で経路内挿（推定・要確認）。残りは手動 |
| 同番号の停留所が1つに統合される | 番号で統合した | **名称で区別**（反対側は別停留所） |
| 時刻が逆行/断片便 | 要予約バス停への寄り道 | 要予約を除外（needs_confirmationに出る） |
| 表に無い「停留所」が出る | 案内文(お問い合わせ欄等)の混入 | needs_confirmationを確認し除外 |

# 付録C：主なスクリプト早見表
| スクリプト | 役割 |
|---|---|
| `extract_timetable_coords.py` / `extract_timetable_excel.py` | Step1 抽出（座標方式 / Excel直接：停留所が行） |
| `extract_excel_transposed.py` | Step1 抽出（転置Excel：停留所が列・便が行。各シート=1方向） |
| `pdf_to_markdown.py` | 画像化PDFのOCR（MinerU 既定 pipeline バックエンド＝数字に正確・速い）→ Markdown |
| `extract_timetable_markdown.py` | Step1 抽出（OCRのMarkdown表→extract.json。通過`\|\|`除外・便ラベル/方向見出し対応） |
| `download_p11.py` | 対象県の国土数値情報P11を取得 |
| `run_pipeline.py` | Step3〜7を一括（生成・座標補完・shapes・翻訳・zip・検証） |
| `merge_stop_coords.py` / `download_official_feed.py` | Step3.5a 旧/公式フィードから座標再利用（名称マッチ）。`official_feed_url` で公式GTFS(BODIK等)を自動DL→再利用。座標のみ・ダイヤは手元優先・CC-BY出典明記（両開にし 対公式224m→0m） |
| `enrich_stops_p11.py` / `enrich_stops.py` / `apply_manual_coords.py` | 座標補完（P11※自治体制約 / Nominatim / 手動）。P11は同名複数候補を `--review-csv` で要確認リスト化 |
| `select_ambiguous_by_route.py` | Step3.5b2 同名複数候補を経路上のあるべき位置に最も近い実在候補へ自動選択（既定ON・要確認の一歩先） |
| `reject_geom_outliers.py` | Step3.5d2 経路から大きく外れる座標（同名誤マッチ）を棄却（既定ON） |
| `interpolate_coords.py` | Step3.5e 経路内挿で推定座標を補完（要確認・外れ値ガード付き） |
| `classify_coord_confidence.py` | Step3.5f 座標を確定/要確認/未補完に分類（補完源＋経路整合）。**第3相＝独立な複数ソース(公式/P11/Nominatim)が近接一致したら確定に昇格**（`--agree-m` 既定100m）。`座標_信頼度.csv` 出力。推測座標を確定として黙って出さない（官公庁用途） |
| `generate_calendar_dates.py` | Step3.6 祝日・運休日を `calendar_dates` に展開（祝日は内閣府CSV一次データ・年末年始/お盆は範囲指定時のみ）。config の `holiday_syukujitsu`／`holiday_nenmatsu`／`holiday_obon` 指定時に自動実行 |
| `verify_stop_times_vs_extract.py` | Step7c 内部整合検証（抽出時刻↔stop_times） |
| `detect_time_anomalies.py` | Step7d 時刻アノマリ検出（便内逆行＋便間パターン外れ）。OCR誤読の疑いを修正候補つきで `時刻アノマリ.json` 出力（自動修正はしない） |
| `validate_gtfs.py` / `validate_gtfs_jp_extensions.py` | MobilityData検証 / JP拡張検証 |
| `make_map_view.py` | 地図ビュー（停留所・経路・便の可視化） |
| `make_gtfs_viewer.py` | Step4c GTFSビューア生成（単一HTML・7タブ：路線一覧/時刻表/運賃表/路線図/カレンダー/バス停/データチェック） |

---
*本ガイドは SKILL.md / README.md を土台に、人間の導線とLLM司令書を1枚へ統合したもの。詳細手順は README.md を参照。*
